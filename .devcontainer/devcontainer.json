{
	"name": "Go",
	"image": "mcr.microsoft.com/devcontainers/go:2-1.26-trixie",
	"customizations": {
		"vscode": {
			"extensions": [
				"vivaxy.vscode-conventional-commits"
			]
		}
	},
	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y gcc libgtk-3-dev libayatana-appindicator3-dev && go mod tidy",
}